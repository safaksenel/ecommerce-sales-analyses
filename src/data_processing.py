"""Data processing utilities for ecommerce sales analyses."""

import pandas as pd


def load_data(path: str) -> pd.DataFrame:
    """Load CSV data from `path` into a DataFrame (placeholder)."""
    return pd.read_csv(path)
